services:
  api:
    build: .
    image: whale-tracker:local
    command:
      - whale-tracker
      - api
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --no-reload
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped

  scheduler:
    build: .
    image: whale-tracker:local
    command:
      - whale-tracker
      - schedule
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped

  cli:
    build: .
    image: whale-tracker:local
    entrypoint:
      - whale-tracker
    command:
      - --help
    profiles:
      - tools
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
